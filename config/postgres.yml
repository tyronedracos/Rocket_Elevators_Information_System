# config/fourth_dimension_database.yml
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: postgres

test:
  <<: *default
  host: localhost
  database: postgres

production:
  <<: *default
  database: postgres